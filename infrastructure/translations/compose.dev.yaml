services:
    tms:
        build:
            target: dev

        image: budget-forecast/tms:dev
        volumes:
            - ./projects:/app/projects:ro
            - ./server:/app/server:ro
        develop:
            watch:
                - path: ./server
                  action: restart
