{
  "name": "@budget-forecast/translations",
  "version": "0.0.0",
  "description": "Translations infrastructure — a mock TMS that serves the project locale files over HTTP",
  "private": true,
  "author": "Xifré Font",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "dev": "node --watch server/src/index.ts",
    "serve": "node server/src/index.ts",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f tms",
    "docker:rebuild": "docker compose up -d --build",
    "docker:dev": "docker compose -f compose.yaml -f compose.dev.yaml up -d --build",
    "lint": "nx eslint:lint",
    "typecheck": "tsc --noEmit",
    "test": "vitest . --watch=false",
    "test:dev": "vitest ."
  },
  "nx": {
    "tags": ["type:infra", "scope:infra"]
  },
  "dependencies": {
    "@hono/node-server": "catalog:",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@budget-forecast/configs": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
