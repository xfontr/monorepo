{
    "name": "monorepo",
    "version": "0.0.1",
    "description": "",
    "author": "Xifré Font",
    "license": "ISC",
    "packageManager": "pnpm@12.4.2",
    "engines": {
        "node": "^22.18.0 || >=24.12.0"
    },
    "scripts": {
        "quick-start": "husky && pnpm test:coverage && pnpm exec nx collect @monorepo/developer-portal",
        "dev": "pnpm --filter @monorepo/scripts run dev",
        "lint": "nx affected -t lint",
        "typecheck": "nx affected -t typecheck",
        "build": "nx affected -t build",
        "test": "nx affected -t test",
        "test:coverage": "nx run-many -t test:coverage && pnpm --filter @monorepo/scripts run coverage",
        "graph": "nx graph",
        "issue:add": "pnpm --filter @monorepo/scripts run issue:add",
        "issue:pick": "pnpm --filter @monorepo/scripts run issue:pick",
        "issue:ship": "pnpm --filter @monorepo/scripts run issue:ship",
        "agents:sync": "pnpm --filter @monorepo/scripts run agents:sync",
        "docs:drift": "pnpm --filter @monorepo/scripts run docs:drift",
        "docs:map": "pnpm --filter @monorepo/scripts run docs:map",
        "review:version": "pnpm --filter @monorepo/scripts run review:version",
        "release": "nx release --skip-publish",
        "release:dry": "nx release --skip-publish --dry-run"
    },
    "devDependencies": {
        "@commitlint/cli": "^21.1.0",
        "@commitlint/config-conventional": "^21.1.0",
        "@nx/eslint": "23.2.1",
        "@nx/js": "23.2.1",
        "@nx/nuxt": "23.2.1",
        "@nx/vite": "23.2.1",
        "@nx/vitest": "23.2.1",
        "husky": "^9.1.7",
        "nx": "23.2.1"
    }
}
