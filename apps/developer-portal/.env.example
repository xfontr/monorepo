# The workspace's GitHub repo. Three things read it: the sidebar footer link, the link a doc's
# non-markdown target renders as, and the issues endpoint, which is this host with `api.` in front.
NUXT_PUBLIC_REPO_URL=
