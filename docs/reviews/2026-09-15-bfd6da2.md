# 📊 Review — 2026-09-15

Commit `bfd6da2` on `feature/monorepo/65-nuxt-types` · Method v2 · Previous:
[2026-09-10](./2026-09-10-bfd6da2.md)

**A calibration re-score, not a review.** Same commit and same tree as
[2026-09-10](./2026-09-10-bfd6da2.md), re-scored under method v2 by applying the new caps to that
review's **own cited evidence** — no fresh read of the code, no new findings, no card moved for a
reason v1 could already see. [`METHOD.md`](./METHOD.md) owes this row: a version says two totals
aren't comparable, and only two rows at one sha say by how much. **4.2/5** against v1's 4.4, and the
whole 0.2 is the method. One cap fires — the test-density floor
[`0012-review-rubric-validity.md`](../spikes/0012-review-rubric-validity.md) adopted, the only new
cap in v2 that scores anything.

## 🧮 Scores

| Card | Score | Δ | Verdict |
| --- | --- | --- | --- |
| 🧱 Architecture | 5/5 | = | No v2 cap touches it; v1's attempted breakage still stands |
| 🧩 Implementation | 5/5 | = | No v2 cap touches it; v1's clean sweep of every greppable cap still stands |
| 🧪 Testing | 3/5 | ↓1 | `apps/tech-docs` at 4.6 modules per spec, over the density floor |
| ⚙️ Tooling & DX | 5/5 | = | No v2 cap touches it |
| 📚 Documentation | 2/5 | = | Already at the v1 cap; v2 adds nothing below it |
| 🤖 Agent setup | 3/5 | = | No v2 cap touches it |
| 📋 Process & delivery | 5/5 | = | No v2 cap touches it |
| **Total** | **4.2/5** | ↓0.2 | |

Δ is against [2026-09-10](./2026-09-10-bfd6da2.md) — **the same tree**, so every Δ in this table is a
method change and none of it is the repo. Weights and the arithmetic live in
[`SCORECARDS.md`](./SCORECARDS.md).

## 🃏 Cards

### 🧱 Architecture — 5/5

Unchanged and not re-argued. v1 cited `boundaries.ts` matching the README row for row,
`@nx/enforce-module-boundaries` at `"error"`, a clean grep of `content`/`i18n`'s `core/`, and
`apps/tech-docs/tools/lib/invariants.spec.ts:63-82` pinning `compareTagTables` against a drifted
pair — a real attempted breakage, which is what a 5 costs. v2 changed no anchor and no cap on this
card. The spike's finding about this card is that nothing in the rubric asks whether the code
*inside* a correctly drawn boundary was written with care, and v2 does not answer it — the one
mechanism that would have was rejected — so the 5 stands on the layering alone.

| Costs a point | Evidence |
| --- | --- |
| Nothing — v2 adds no cap to this card, and v1's sweep is carried forward unchanged | `2026-09-10-bfd6da2.md` § 🧱 Architecture |

### 🧩 Implementation — 5/5

Unchanged, and the card the spike was most about. v1 scored a clean 5 on a sweep that found no
`any`, no ts-escape, no `eslint-disable`, no single-caller abstraction and no swallowed error path —
every one of those a greppable predicate, and every one still passing at this sha. v2 considered a
per-package attestation to give the card a cap no predicate could satisfy and
[rejected it](../spikes/0012-review-rubric-validity.md): a one-collaborator repo has nobody to sign
it but the author.

So the 5 here means the greps are clean and nothing else, which is what the spike says to read it
as. The card carries 20 of the 100 points and cannot move on judgement at all — that is the open
gap, not a finding against this tree.

| Costs a point | Evidence |
| --- | --- |
| Nothing — every cap on this card is a grep and all of them come back clean | `2026-09-10-bfd6da2.md` § 🧩 Implementation |

### 🧪 Testing — 3/5 (↓1)

The card the spike's empirical test was about. v1 scored 4 and deducted two named files —
`apps/tech-docs/tools/collect/metrics.ts` and `deps.ts` — while the same review docked two points on
📚 Documentation for one stale command string. Under v2 the density floor fires first:
`apps/tech-docs` carries 23 `.ts` modules against 5 spec files, 4.6 per spec, over the floor of 4.
Every other project is under it — `scripts` at 3.1, `huella-legal` at 3.0, `content` at 1.2 — so the
cap discriminates rather than firing everywhere.

v1's own finding is unchanged and unpromoted: the untested parsing logic is real, and the cap is a
ceiling over it rather than a second deduction for the same fact.

| Costs a point | Evidence |
| --- | --- |
| `apps/tech-docs` is over the density floor — 23 modules, 5 specs, 4.6 per spec against a floor of 4 | `git ls-tree -r bfd6da2 -- apps/tech-docs`, filtered as `collect-facts.sh` filters; contrast `infrastructure/scripts` 37/12 = 3.1 |
| Real parsing logic with no spec and no named reason (carried from v1, now under the cap) | `apps/tech-docs/tools/collect/metrics.ts:50-57,93-143`, `deps.ts:37-94` |

### ⚙️ Tooling & DX — 5/5

Unchanged. v1's 5 rests on a nameable breakage — drop `nuxt-prepare` from `typecheck`'s `dependsOn`
and `vue-tsc` fails on the missing `.nuxt/tsconfig.*.json`, caught by `.husky/pre-push:159-165` and
`ci.yml:32`. v2 adds no cap here. Note for the next real review, not scored now: v2 itself adds a CI
step (`check-docs` enforcing the method version) and a root script, which is this card's business
whenever the tree is next read for its own sake.

| Costs a point | Evidence |
| --- | --- |
| Nothing — v2 adds no cap to this card | `2026-09-10-bfd6da2.md` § ⚙️ Tooling & DX |

### 📚 Documentation — 2/5

Unchanged, and the reason it doesn't move is the finding the spike opened with. v1 capped this at 2
on `apps/tech-docs/README.md:8,105-106` naming two commands that don't exist — one stale string,
two points, 0.20 off the total. v2 deliberately did **not** retune that: the swing was the rubric
working as written, and treating it as miscalibration was the option the spike rejected. What v2
changes is the other side of the comparison — a stale command string and an unread 4,201-line app
now cost comparable amounts, where under v1 the string cost twice as much.

| Costs a point | Evidence |
| --- | --- |
| Two documented commands that don't exist (carried from v1, unchanged) | `apps/tech-docs/README.md:8,105-106` vs `apps/tech-docs/package.json:6-14` |

### 🤖 Agent setup — 3/5

Unchanged. v1 capped at 3 because "never raise git" — CLAUDE.md's loudest ban — has no deny entry
and no hook. v2 adds no cap here.

| Costs a point | Evidence |
| --- | --- |
| "Never raise git" has no deny entry and no hook (carried from v1, unchanged) | `.claude/settings.json:4-32` |

### 📋 Process & delivery — 5/5

Unchanged. Four nameable breakages, each caught: a hand-edited version fails `check-invariants.sh`,
an added `TODO` fails `.husky/pre-push:81-91`, a non-conventional subject fails `commitlint`, a
mistyped branch fails the pre-push regex. v2 adds no cap here.

| Costs a point | Evidence |
| --- | --- |
| Nothing — v2 adds no cap to this card | `2026-09-10-bfd6da2.md` § 📋 Process & delivery |

## 🔗 Across the cards

The cross-card pass is v2's first change and this row is the one it was built to catch. Grouping
v1's seven card reports by project rather than by card, **`apps/tech-docs` is named on three of
them** — the whole of 📚 Documentation's cap, the whole of 🧪 Testing's deduction, and the bulk of
the 14,301 added lines ⚙️ Tooling & DX scored the wiring of. No single card saw a 4,201-line app
landing in 62 commits; each saw its own survivable shard, which is precisely the fragmentation
`repo-review-card.md`'s stay-inside-your-card rule produces and the reason it now needs a reader
above it.

This row costs points **once**, on 🧪 Testing, where the density floor already scores it. It is
recorded here because the pattern is the finding and the floor is only the part of it a predicate
can reach.

| Finding | Shows up on | Scored on | Evidence |
| --- | --- | --- | --- |
| `apps/tech-docs` landed the workspace's largest codebase in one branch, thinly specced and self-documented wrongly; each card saw one shard | 🧪 Testing, 📚 Documentation, ⚙️ Tooling & DX | 🧪 Testing | `metrics.ts:50-57,93-143`, `deps.ts:37-94`; `apps/tech-docs/README.md:8,105-106`; 23 modules / 5 specs; +14301/−2307 over 62 commits |

## 🔍 Evidence

Every figure below is v1's, re-read rather than re-collected — the tree is identical, so re-running
`collect-facts.sh` would return the same numbers against a different HEAD and make the comparison
worse, not better. The rows marked *(v2)* are derived from `bfd6da2` directly.

| Fact | Value |
| --- | --- |
| Targets, whole workspace | `lint` pass · `typecheck` pass · `test` pass · `build` pass, 9 projects |
| Source files (ts/vue, excl. specs) | 178 |
| Specs / test cases | 51 / 367 |
| `any`, ts escapes, `eslint-disable` in non-spec source | 0 / 0 / 0 |
| Modules per spec, `apps/tech-docs` *(v2)* | 23 / 5 = 4.6 — over the floor of 4 |
| Modules per spec, every other project *(v2)* | `scripts` 3.1 · `huella-legal` 3.0 · `translations` 1.8 · `i18n` 1.3 · `content` 1.2 · `observability` 0.5 · `configs` none |
| Commits / diff since the previous review | 0 — same commit |

## 🎯 What would move the total

| Action | Card | Points | Filed |
| --- | --- | --- | --- |
| Fix `apps/tech-docs/README.md`'s command table | 📚 Documentation | 2 → 4, +0.20 | — |
| Bring `apps/tech-docs` under 4 modules per spec, starting with the parsing logic v1 already named | 🧪 Testing | 3 → 4, +0.15 | — |
| Add `deny` entries or a hook for git write commands | 🤖 Agent setup | 3 → 4, +0.10 | — |

Nothing on this list moves 🧱 Architecture or 🧩 Implementation, which carry 40 of the 100 points
between them — the two cards v2 leaves scoring on greps alone. Ordered by points per unit of
work; anything worth doing gets filed (`pnpm issue:add`) or becomes a
[spike report](../spikes/README.md).
