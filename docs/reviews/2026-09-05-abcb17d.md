# 📊 Review — 2026-09-05

Commit `abcb17d` on `feature/48-refactor-the-issue-infra-package` · Rubric v1 · Previous:
[2026-09-04](./2026-09-04-c1025f3.md)

A feature branch, so this scores work in progress: 40 commits of the `@monorepo/scripts` issue
refactor landed since yesterday's review, and it's re-scored today at the user's request rather than
against `master`. **4.2/5**, still carried by a clean 5 on Architecture. The point that actually
moved is 🤖 Agent setup, 3 → 4, now that [`check-invariants.sh`](../../.claude/hooks/check-invariants.sh)
gates a hardcoded endpoint — the exact gap the last review named. What's left is concentrated where it
already was: Implementation's `no-explicit-any` still sits at warn, not error, and Testing still can't
reach the one branching page under `app/`.

## 🧮 Scores

| Card | Score | Δ | Verdict |
| --- | --- | --- | --- |
| 🧱 Architecture | 5/5 | = | The tag graph and `core/` isolation still hold under this branch's refactor, unmoved |
| 🧩 Implementation | 4/5 | = | Zero escapes, honest error paths; `no-explicit-any` is warn, so a regression wouldn't fail CI |
| 🧪 Testing | 4/5 | = | Error paths outnumber happy paths everywhere but `articles/index.vue`'s status-mapping branch |
| ⚙️ Tooling & DX | 4/5 | = | Cache outputs and hook/CI parity are correct and documented, not diffed automatically |
| 📚 Documentation | 4/5 | = | Accurate across all eight projects; this branch's own `scripts/CLAUDE.md` breaks the link-first convention |
| 🤖 Agent setup | 4/5 | ↑1 | The endpoint-ban gap is closed; the lifecycle-script check now covers only `packages/*` |
| 📋 Process & delivery | 4/5 | = | Branch, commit and spike trail enforced; no live breakage attempted this session |
| **Total** | **4.2/5** | ↑0.1 | |

Δ is against [2026-09-04](./2026-09-04-c1025f3.md). Weights and the arithmetic live in
[`SCORECARDS.md`](./SCORECARDS.md).

## 🃏 Cards

### 🧱 Architecture — 5/5

The layering is still a property of the code, not a picture, and this branch's refactor didn't touch
the rule that makes it so. [`boundaries.ts`](../../packages/configs/src/eslint/lib/boundaries.ts)
and the [root README's table](../../README.md#-architecture--boundaries) list the same eight tags
verbatim, `@nx/enforce-module-boundaries` runs as `"error"`, and
[`coreIsolation.ts`](../../packages/configs/src/eslint/lib/coreIsolation.ts) is composed into every
project's real lint target — grepping `packages/content/src/core` and `packages/i18n/src/core` for
`@nuxt/*`/`h3`/`nitropack` still returns zero hits. `content` and `i18n` diverge only where their
`CLAUDE.md`s name the divergence as deliberate (relay-vs-map error handling), not by accident.
`apps/huella-legal` still reaches only composables, never a package's internal path, and
`infrastructure/scripts` — the package actually rewritten this branch — holds the same
entry → command → adapter → domain shape its own docs describe.

| Costs a point | Evidence |
| --- | --- |
| Nothing — sweep found no remaining gap | Tag tables agree; the rule is `error`; zero `process.env`/vendor-SDK reach inside `content`/`i18n`'s `core/`; the 4 `process.env` hits are all inside an `adapters/` folder or a standalone service's own config |

### 🧩 Implementation — 4/5

Still zero `any`, zero `@ts-ignore`/`@ts-expect-error`, zero `eslint-disable` across 122 source
files. Error paths stay honest: `issue/adapters/gh.ts:47-61` degrades a failed `gh project list` to
`[]`, but `issue/pick.ts:41-46` surfaces that as an explicit warning rather than silence, and
`shared/adapters/exec.ts:5-34` defends against PATH-shadowing and flag injection with a spec pinning
both. What holds this off a 5 is unchanged from a structural standpoint: `no-explicit-any` is only a
`warn` inside `tseslint.configs.recommendedTypeChecked`
([`packages/configs/src/eslint/node.ts:12`](../../packages/configs/src/eslint/node.ts)), and no
project's script passes `--max-warnings 0`. Today's zero-`any` count is real, but a regression
wouldn't fail lint or CI — the calibration rule's "name the tooling that catches it" test has no
answer for that specific mistake.

| Costs a point | Evidence |
| --- | --- |
| A regression to an explicit `any` would not fail any gate | `packages/configs/src/eslint/node.ts:12` extends `recommendedTypeChecked` unmodified; no `package.json` script adds `--max-warnings 0` |

### 🧪 Testing — 4/5

286 cases across 42 specs, and most of the suite clears the "name the breakage and the test that
catches it" bar: `packages/i18n/.../translations.get.spec.ts:58-124` distinguishes 404/500/502 and a
mismatched locale as separate cases, `apps/huella-legal/server/plugins/observability.spec.ts:242-331`
pins 4xx-vs-5xx span marking. Providers are exercised through injected ports, never a module mock
asserted against itself. `infrastructure/scripts`' two `--passWithNoTests` hits cover only thin
`adapters/` wrappers over `gh`/`git`/prompts — every file its own README calls "real logic"
(`issue/domain/branch.ts`, `map/domain/render.ts`, `coverage-report/domain/merge.ts`, …) has a
matching spec. The one real gap is unchanged from the last review:
`apps/huella-legal/app/pages/articles/index.vue:16-33` maps a 400 to a 404 and relays everything else
as a 502 — genuine branching — with no spec, because a spec under `app/` needs a Vue preset the app
doesn't have yet ([`writing-tests`](../../.claude/skills/writing-tests/SKILL.md:91-94)). Named and
deliberate, which is the anchor-4 test, not the anchor-5 one.

| Costs a point | Evidence |
| --- | --- |
| Real branching logic in the highest-traffic page has no spec and no path to one today | `apps/huella-legal/app/pages/articles/index.vue:16-33`; the gap is written down in `.claude/skills/writing-tests/SKILL.md:91-94` |
| Same class, smaller stakes | `apps/huella-legal/app/plugins/devWarnings.client.ts:1-16` — a real conditional, no spec |

### ⚙️ Tooling & DX — 4/5

Nx caching is honest: `nx.json:29-48`'s declared `outputs` for `build` and `build-storybook` match
where Nuxt and Storybook actually write. The pre-push/CI gap is real but named in two places
([`README.md:130,153`](../../README.md), `CLAUDE.md`), the fresh-clone step
(`git config core.hooksPath .husky`) is documented with its reason, and the `PostToolUse` eslint-fix
hook settles formatting before it reaches review. What keeps this at 4 rather than 5: nothing
verifies that a declared `outputs` path keeps matching reality if a project's build config changes,
and nothing diffs `.husky/pre-push` against `.github/workflows/ci.yml` to keep the documented parity
honest going forward — both are correct today and unenforced tomorrow, which is the anchor-4 test
exactly.

| Costs a point | Evidence |
| --- | --- |
| Cache-output accuracy and hook/CI parity are documented, not diffed by anything | `nx.json:29-48` vs `apps/huella-legal/package.json:7`/`packages/ui/package.json:17` (correct today, no check going forward); `.husky/pre-push:122-155` vs `.github/workflows/ci.yml:32-37` (gap named, not diffed) |

### 📚 Documentation — 4/5

Every claim checked against code held: `infrastructure/scripts/README.md`'s structural claims match
the actual `src/{issue,drift,map,coverage-report,shared}/**` tree this branch rewrote, and the root
README's boundary table still agrees with `boundaries.ts` verbatim. The systemic gap named last
review is unchanged — `check-invariants.sh:18-76` only hard-checks three specific pairings (the tag
table, the review-history row, a new project's three places), so nothing catches a documented path
that quietly stops existing. New this review: `infrastructure/scripts/CLAUDE.md`, rewritten on this
branch, breaks the link-first convention every other project's `CLAUDE.md` follows — it restates
several paragraphs of `README.md:20-97` (the folder shape, the `exec.ts` rationale, the
`--passWithNoTests` justification) that six other `CLAUDE.md`s (`packages/ui`, `packages/content`,
`packages/i18n`, `packages/observability`, `apps/huella-legal`, `infrastructure/translations`) handle
by linking and stating only what the README can't enforce.

| Costs a point | Evidence |
| --- | --- |
| No tooling validates a documented path, link, or CLAUDE.md/README duplication in general | `.claude/hooks/check-invariants.sh:18-76` covers only three named pairings — same gap as the last review |
| This branch's own `CLAUDE.md` rewrite duplicates its README instead of linking it | `infrastructure/scripts/CLAUDE.md:11-33` vs `infrastructure/scripts/README.md:20-97`; contrast `packages/ui/CLAUDE.md:11-12` ("this file doesn't restate it") |

### 🤖 Agent setup — 4/5 (↑1)

The mover this review. The gap that capped the last review at 3 — "never write a real endpoint, URL,
token or instance ID into the repo" had neither a deny entry nor a hook — is closed:
`check-invariants.sh:78-100` now greps a pushed diff for a literal `https?://` in non-spec
source/config, with the allowlist the last review warned would be needed (`$schema` keys, doc
links) so it doesn't cry wolf. The other three findings from last time hold clean too: the
build-step/lifecycle-script check, the hand-edited-`version` check, and skill descriptions all still
match what `CLAUDE.md` claims — checked all 13 skills against their trigger table row, no mismatch.
What remains: the lifecycle-script ban is stated repo-wide in `CLAUDE.md`, but
`check-invariants.sh:24-38` only cases on `packages/*/package.json` — a `postinstall` added to
`apps/huella-legal/package.json` or an `infrastructure/*` project's would go uncaught, and that
scoping isn't written down as deliberate anywhere.

| Costs a point | Evidence |
| --- | --- |
| The lifecycle-script ban is enforced for one of three project roots it names, silently | `.claude/hooks/check-invariants.sh:24-38` cases only on `packages/*/package.json`; `CLAUDE.md`'s ban is repo-wide |
| "Don't reach for a validation/utility library" has neither a deny entry nor a hook | `CLAUDE.md`'s "things that look reasonable" bullet; no matching case in `check-invariants.sh` or `.claude/settings.json` — though this one is a judgment call a mechanical check likely can't make well |

### 📋 Process & delivery — 4/5

Everything gate-able still is. `.husky/commit-msg:12-25` threads the branch's issue number into every
subject automatically — visible in this branch's own `abcb17d`, `2a8b824`, `370a588` reading
`type: [48] …` without anyone typing the number — and every commit's type matches its diff
(`docs: [48] refactor spike` adds the spike file; `refactor: [48] add adapters and domain folders`
matches the actual `adapters/`/`domain/` split). The architectural fork this branch resolves has a
spike report ([`docs/spikes/0048-scripts-architecture.md`](../spikes/0048-scripts-architecture.md)),
closing the empty-`docs/decisions/` gap the last review flagged (since renamed to `docs/spikes/`).
No hand-edited version or `CHANGELOG.md` on this branch. What keeps it at 4, matching the last
review's own reasoning for this card: nothing here is a fresh adversarial test of the hooks this
session, only confirmation they're well-formed.

| Costs a point | Evidence |
| --- | --- |
| No breakage attempted this session against the enforcement chain | Confirmed the hooks and spike trail are correct by reading them, not by trying to break one — same reasoning the last review gave for holding this card at 4 |

## 🔍 Evidence

| Fact | Value |
| --- | --- |
| Targets, whole workspace | `lint` pass · `typecheck` pass · `test` pass · `build` pass, 8 projects |
| Source files (ts/vue, excl. specs) | 122 |
| Specs / test cases | 42 / 286 |
| `any`, ts escapes, `eslint-disable` in non-spec source | 0 / 0 / 0 |
| Flagged comments (`TODO`/`FIXME`) | 0 |
| Skipped or focused specs · `--passWithNoTests` over real logic | 0 · 0 (2 hits, both over adapter wrappers only) |
| `process.env` reads | 4, all inside an `adapters/` folder or a standalone service's own config |
| Source files over 200 lines | 1 — `apps/huella-legal/app/pages/articles/index.vue` (291) |
| Commits / diff since the last review | 40 commits, 120 files changed (+4042/−810) |
| Endpoint-ban check (new) | `check-invariants.sh:78-100` rejects a literal `https?://` in pushed non-spec source/config |

## 🎯 What would move the total

| Action | Card | Points | Filed |
| --- | --- | --- | --- |
| Flip `no-explicit-any` (and the other `recommendedTypeChecked` type-safety rules) to `error`, or add `--max-warnings 0` to the lint script | 🧩 Implementation | 4 → 5, +0.20 | — |
| Scope the lifecycle-script check in `check-invariants.sh` to every `package.json`, not just `packages/*` | 🤖 Agent setup | 4 → 5, +0.10 | — |
| Rewrite `infrastructure/scripts/CLAUDE.md` to link-first like the other six projects' | 📚 Documentation | 4 → 5, +0.10 | — |
| Diff `.husky/pre-push`'s target list against `.github/workflows/ci.yml`'s (or accept the `build` gap explicitly and stop calling it a gap) | ⚙️ Tooling & DX | 4 → 5, +0.15 | — |
| Give `app/` a Vue test preset so `articles/index.vue`'s status-mapping branch can be pinned | 🧪 Testing | 4 → 5, +0.15 | — |

Ordered by points per unit of work. This table is a finding, not a tracker: anything worth doing gets
filed (`pnpm issue:add`) or becomes a [spike report](../spikes/README.md), and the row records which.
