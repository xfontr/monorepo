# 📊 Review — 2026-09-15

Commit `aea557e` on `feature/technical-docs/103-broken-links` · Method v2 · Previous:
[2026-09-15](./2026-09-15-bfd6da2.md)

A feature branch one commit ahead of `master` on a clean tree, so this is effectively master's tip
plus the 0013 spike correction. **3.9/5** against the v2 re-score's 4.2, and none of the 0.3 is a
regression: ⚙️ Tooling & DX and 🧩 Implementation both lose points to defects that were already
there at `bfd6da2` and went unrecorded, while 🧪 Testing gains one back because the density cap that
fired at 4.6 modules per spec no longer does. The single change that would move the total most is
one line — declaring `typecheck`'s `.tsbuildinfo` output in [`nx.json`](../../nx.json) lifts a cap
worth +0.30.

## 🧮 Scores

| Card | Score | Δ | Verdict |
| --- | --- | --- | --- |
| 🧱 Architecture | 5/5 | = | Ten tag rows agree, the rule is `error`, and core isolation bans the Nuxt runtime outright |
| 🧩 Implementation | 4/5 | ↓1 | Every grep is clean, but four hand-rolled tables duplicate markup nothing would catch |
| 🧪 Testing | 4/5 | ↑1 | `apps/tech-docs` is at 3.7 modules per spec, back under the density floor |
| ⚙️ Tooling & DX | 3/5 | ↓2 | `typecheck` is cached without declaring the `.tsbuildinfo` it writes |
| 📚 Documentation | 2/5 | = | `packages/configs/README.md` says a file doesn't exist; it does |
| 🤖 Agent setup | 3/5 | = | "Never raise git" is still the one ban with no deny entry and no hook |
| 📋 Process & delivery | 5/5 | = | All seven gates pass, and every one has a nameable breakage |
| **Total** | **3.9/5** | ↓0.3 | |

Δ is against [2026-09-15](./2026-09-15-bfd6da2.md) — the same method, so every Δ here is the repo
and none of it is the rubric. `(5×20 + 4×20 + 4×15 + 3×15 + 2×10 + 3×10 + 5×10) / 100 = 385/100 =
3.85`, one decimal **3.9**. Weights and the arithmetic live in [`SCORECARDS.md`](./SCORECARDS.md).

## 🃏 Cards

### 🧱 Architecture — 5/5

The layering is still a property of the code. All ten rows of `depConstraints` in
[`boundaries.ts`](../../packages/configs/src/eslint/lib/boundaries.ts) match the
[root README's table](../../README.md#-architecture--boundaries) tag for tag and consumer for
consumer, and `@nx/enforce-module-boundaries` is registered at `"error"`
(`packages/configs/src/eslint/lib/boundaries.ts:8`). A second rule does the work the table can't:
`coreIsolation.ts:1-9` bans `@nuxt/*`, `@nuxtjs/*`, `nitropack`, `h3` and `#nuxt/*` under
`**/src/core/**/*.ts` at `error` severity, with a message naming the port that replaces them — so
"a domain that doesn't know its transport" is a lint failure, not an aspiration. Both breakages are
nameable and both are caught: importing `h3` into `packages/content/src/core/` fails
`no-restricted-imports`, and giving `i18n` a dependency on `content` fails the boundaries rule, each
under `pnpm lint`, which `ci.yml:32` runs on every PR. No package reaches an env var either — the
only `process.env` reads in source are `infrastructure/scripts/src/drift/main.ts`,
`infrastructure/scripts/src/shared/adapters/io.ts` and
`infrastructure/translations/server/src/configs/constants.ts`, all infrastructure, none under
`packages/*`.

| Costs a point | Evidence |
| --- | --- |
| Nothing — sweep found no gap | Ten `depConstraints` rows in `boundaries.ts:11-65` against `README.md:51-60`; severity `error` at `boundaries.ts:8`; `coreIsolation.ts:1-23`; `git grep process.env` over `packages/*` and `apps/*` returns only README and `CLAUDE.md` prose, no source |

### 🧩 Implementation — 4/5 (↓1)

The greps are genuinely clean: zero `any`, zero `@ts-ignore`/`@ts-expect-error`, zero
`eslint-disable` across 189 non-spec source files, with `@typescript-eslint/no-explicit-any` at
`"error"` in both `packages/configs/src/eslint/node.ts:31` and `vue.ts:38` so a regression fails
lint. All 23 `console.*` calls outside specs were traced and every one is deliberate output, not
stray debugging — 13 in `apps/tech-docs/tools/collect/index.ts`, 7 in the new
`apps/tech-docs/tools/check-docs/index.ts`, a dev-server banner in
`infrastructure/translations/server/src/index.ts`, two documented build-time warnings in
`packages/i18n/src/nuxt/module.ts`, and the `console.info` patch for a known Vue bug in
`apps/huella-legal/app/plugins/devWarnings.client.ts`. What costs the point is duplication the card's
caps cannot see: four table blocks in `apps/tech-docs` repeat the same
`overflow-x-auto` / `table.w-full.text-sm` / `thead.text-xs.text-muted.border-b.border-default` /
`tbody.divide-y.divide-default` structure while `@nuxt/ui` is a dependency and `UTable` is never
used anywhere in the app. This is a deduction and **not** the card's cap-3: fixing one leaves the
others inconsistent rather than wrong, and stretching a correctness cap to cover presentation is
what this card's own note warns against.

| Costs a point | Evidence |
| --- | --- |
| The same table markup is hand-rolled four times with a component library installed and unused for it | `apps/tech-docs/app/pages/coverage.vue:56`, `deps.vue:80`, `deps.vue:159`, `scorecards.vue:116`; `@nuxt/ui` at `apps/tech-docs/package.json:26`, `git grep UTable` over `apps/*` returns nothing |
| Nothing catches a regression in what this card measures beyond the greps | No `no-console` rule in `packages/configs/src/eslint/`; the card's own note records why the mechanism that would close this was rejected ([`0012`](../decisions/0012-review-rubric-validity.md)) |

### 🧪 Testing — 4/5 (↑1)

The mover, and the reason is arithmetic rather than new specs. `apps/tech-docs` sat at 4.6 modules
per spec when the v2 re-score applied the new density cap; at this tree it is 26 modules over 7
spec files — **3.7**, under the floor of four — and no other project is close (`content` 1.2,
`i18n` 1.3, `translations` 1.8, `huella-legal` 3.0, `scripts` 3.1). The cap does not fire, and per
the card it is a cap and never a bonus, so the ratio buys nothing beyond lifting it. Nothing else
caps either: zero skipped or focused specs, and the two `--passWithNoTests` hits are both in
`infrastructure/scripts`, whose README names that trade-off on purpose. New code arrived with specs
where the logic is real — `infrastructure/scripts/src/review-version/domain/manifest.spec.ts` sits
beside the domain while the adapters don't, which is the convention that package documents. The
standing gap is unchanged: `apps/tech-docs/tools/collect/metrics.ts` and `deps.ts` hold parsing and
reshaping logic with no spec, and unlike `infrastructure/scripts/README.md:106-112` — which names
exactly which of its own files stay untested and why — `apps/tech-docs/README.md` has no such
section, so the gap is real and undeclared.

| Costs a point | Evidence |
| --- | --- |
| Parsing logic with no spec and no written reason, in a project whose sibling documents the same trade-off | `apps/tech-docs/tools/collect/metrics.ts` and `deps.ts` absent from the spec list (`git ls-files apps/tech-docs/tools`); contrast `infrastructure/scripts/README.md:106-112` |
| Swept clean otherwise | 0 skipped/focused specs; density max 3.7 at `apps/tech-docs`; both `--passWithNoTests` hits covered by `infrastructure/scripts/README.md:108` |

### ⚙️ Tooling & DX — 3/5 (↓2)

Capped by the card's own cap-3, on evidence that is physical rather than inferred.
[`nx.json`](../../nx.json)`:24-28` gives `typecheck` `"cache": true` and no `outputs`, while
`packages/ui/package.json:13` runs `vue-tsc --build` and both `packages/ui/tsconfig.app.json` and
`tsconfig.node.json` set `tsBuildInfoFile` to `./node_modules/.tmp/` — each with a comment saying
`vue-tsc --build` produces the file. Both files exist on disk right now. So a cached target writes
two artifacts inside `{projectRoot}` that Nx never restores on a hit. The harm is a lost incremental
typecheck rather than a wrong answer, which is worth saying plainly — but the cap is a ceiling, not
a deduction weighted by consequence, and the condition is met literally. This is not new: the same
`typecheck` block is byte-identical at `bfd6da2`, so the two reviews that scored this card 5 missed
it. Everything else on the card holds up. `build`, `build-storybook`, `nuxt-prepare` and
`test:coverage` all declare outputs matching what they write; `collect` and `check-docs` correctly
opt out of caching entirely; `sharedGlobals` covers `nx.json` and `pnpm-workspace.yaml`; all four
targets pass across nine projects on a clean tree; and the `core.hooksPath` step a fresh clone needs
is documented in `README.md` and `docs/guides/first-hour.md`.

| Costs a point | Evidence |
| --- | --- |
| A cached target writes two undeclared outputs — the literal cap-3 condition | `nx.json:24-28` (`cache: true`, no `outputs`); `packages/ui/package.json:13`; `packages/ui/tsconfig.app.json` and `tsconfig.node.json` `tsBuildInfoFile`; both `.tsbuildinfo` files present under `packages/ui/node_modules/.tmp/` |
| The one document that claims to be the full local/CI parity account names one gap and there are four | `docs/guides/change-lifecycle.md:57-60` ("plus `build`… that's the one asymmetry") vs `ci.yml:32,37,41,45`, which also runs `docs:map --check`, `review:version --check` and `check-docs`; `.husky/pre-push` runs only `lint`, `test`, `typecheck` |

### 📚 Documentation — 2/5

Capped by the card's 2-rule, on a claim that contradicts a file sitting one directory away.
`packages/configs/README.md:7-8` states that "`apps/huella-legal` and this package have no
`vitest.config.ts` at all" — but `apps/huella-legal/vitest.config.ts` is tracked in git and its
entire contents are `import { vitest } from "@monorepo/configs"; export default
vitest.createNodeConfig();`, which is the factory the same README documents. The claim is the
load-bearing half of that paragraph's argument ("Only the ESLint half reaches all of them"), and it
is the half a reader would rely on: only `packages/configs` itself now lacks a `vitest.config.ts`,
so the sentence names the wrong project and misses the right one. The root README is correct and
defers here rather than repeating it (`README.md:77-78`), which is why this costs one README and not
two. The rest of the card is strong and should not be read as failing: all nine
projects have a substantive README that argues *why*, the `check-docs` gate reports 83 pages and 13
spikes with no findings, and the previous review's `apps/tech-docs` command drift stays fixed.

| Costs a point | Evidence |
| --- | --- |
| A README asserts a file doesn't exist; it does, and it calls this package's own factory | `packages/configs/README.md:7-8` vs `apps/huella-legal/vitest.config.ts` (tracked; calls `vitest.createNodeConfig()`); `git ls-files` shows 8 of 9 projects with a `vitest.config.ts`, `packages/configs` the only one without |
| `infrastructure/scripts/CLAUDE.md` restates its README's layout and naming rules instead of linking them (non-capping) | `infrastructure/scripts/CLAUDE.md` vs `infrastructure/scripts/README.md`; contrast `packages/ui/CLAUDE.md`, which says outright that it doesn't restate the boundary rule |

### 🤖 Agent setup — 3/5

Enforcement is strong everywhere it exists and absent in exactly one place, which is the cap.
`.claude/hooks/check-invariants.sh` blocks the boundaries/README pair drifting, a lifecycle script,
a `build` script under `packages/*`, a hand-edited `version`, a hardcoded endpoint, and a review
file with no history row — each exiting 2 only for its own condition, so nothing cries wolf. The
`deny` list covers `CHANGELOG.md`, `docs/FEATURES.md`, `nx release` and `.env` reads directly. What
caps it: `CLAUDE.md`'s loudest ban — never raise git — has no `deny` entry and no hook. The `allow`
list at `.claude/settings.json:4-19` enumerates seven read-only git verbs and the `deny` list
at `:21-31` contains no git entry at all, while both `PostToolUse` hooks match only `Edit|Write`
and so never see a `Bash` call. The ban carries a carve-out for when the user does ask, so a blanket
deny would over-block — but partial, best-effort enforcement is this repo's own convention for the
other six bans, and here there is none at all.

| Costs a point | Evidence |
| --- | --- |
| The one `CLAUDE.md` ban with no deny entry and no hook, where a partial one is possible | `CLAUDE.md` 🚫 section; `.claude/settings.json:4-19` (read-only git verbs only), `:21-31` (no git entry); hooks at `:35-53` match `Edit\|Write` only |
| `CLAUDE.md` overstates its own enforcement in the file an agent reads for the rule | `CLAUDE.md:5-6` claims `.claude/settings.json` "denies what the prose below forbids"; four of the bans are caught by `check-invariants.sh` instead and the git ban by neither |

### 📋 Process & delivery — 5/5

Every gate-able thing is gated and each has a nameable breakage, and unusually for this card the
whole set was executed rather than read. `pnpm review:version --check` reports method v2 with four
artifacts unchanged, `pnpm docs:map --check` reports `docs/FEATURES.md` up to date, and
`pnpm exec nx check-docs @monorepo/tech-docs` reports 83 pages and 13 spikes with no findings — so
the three CI-only gates are green on this tree, alongside `lint`, `typecheck`, `test` and `build`.
All thirteen spike reports carry YAML frontmatter, which is what the frontmatter linter checks.
Versions and changelogs are derived, not typed: `packages/ui/CHANGELOG.md:1` is a `0.1.5` block in
`nx release` shape with commit links and a contributors section, matching
`packages/ui/package.json:3`, and `check-invariants.sh` exits 2 on a `version` moved by hand. Commit
types match their diffs across the window — `feat:` for the doc linter and the pipeline wiring,
`docs:` for the spike work, `fix:` for the actor correction — and the branch matches the pre-push
regex.

| Costs a point | Evidence |
| --- | --- |
| Nothing — sweep found no hand-edited version or changelog, no unreported fork, no type/diff mismatch, no failing gate | `pnpm review:version --check`, `pnpm docs:map --check` and `nx check-docs` all exit 0; 13/13 spikes carry frontmatter; `packages/ui/CHANGELOG.md:1` vs `package.json:3`; `git log --oneline -14` |

## 🔗 Across the cards

One pattern fragments across two cards and is scored on one. `apps/tech-docs` is the project
every deduction in this review points at: it owns the four duplicated table blocks (🧩), the two
untested parsing modules (🧪), and six of the seven files over 200 lines. Read together that is not
three findings but one — the dashboard is the youngest and least-supervised surface in the
workspace, and it is the only project whose README does not name what it left undone, which is
precisely the habit that keeps the other eight scoring well. It costs points on 🧪 Testing, where
the undeclared gap is the card's own subject; the 🧩 row is a separate defect that happens to live
in the same project.

| Finding | Shows up on | Scored on | Evidence |
| --- | --- | --- | --- |
| `apps/tech-docs` is the one project that doesn't write down what it left undone | 🧪 Testing, 🧩 Implementation | 🧪 Testing | Untested `tools/collect/metrics.ts` and `deps.ts` with no trade-off section in `apps/tech-docs/README.md`, against `infrastructure/scripts/README.md:106-112`; duplicated markup at `coverage.vue:56`, `deps.vue:80,159`, `scorecards.vue:116`; six of the seven files over 200 lines |
| Two reviews scored ⚙️ Tooling 5/5 over a defect present the whole time | ⚙️ Tooling & DX | ⚙️ Tooling & DX | `git diff bfd6da2..HEAD -- nx.json` touches only `check-docs`; the `typecheck` block is byte-identical at both commits |

## 🔍 Evidence

| Fact | Value |
| --- | --- |
| Targets, whole workspace | `lint` pass · `typecheck` pass · `test` pass · `build` pass, 9 projects |
| CI-only gates, run by hand for this review | `review:version --check` pass (v2, four artifacts unchanged) · `docs:map --check` pass · `check-docs` pass (83 pages, 13 spikes, no findings) |
| Source files (ts/vue, excl. specs) | 189 |
| Specs / test cases | 54 / 407 |
| Modules per spec, highest | `apps/tech-docs` 3.7 — under the floor of 4 that capped it at the last review (4.6) |
| `any`, ts escapes, `eslint-disable` in non-spec source | 0 / 0 / 0 |
| Flagged comments (`TODO`/`FIXME`) | 0 |
| `console.*` outside specs | 23, all traced to CLI output, a dev banner, two build-time warnings or a documented Vue-bug patch |
| `process.env` reads | 4, none under `packages/*` |
| Skipped or focused specs · `--passWithNoTests` over real logic | 0 · 0 (2 hits, both in `infrastructure/scripts`, named in its README) |
| Source files over 200 lines | 7 — six of them in `apps/tech-docs` |
| Files with no sibling spec (filtered) | 69 |
| Commits / diff since the last review's commit | 29 commits, 106 files (+4226/−845) |

## 🎯 What would move the total

| Action | Card | Points | Filed |
| --- | --- | --- | --- |
| Add `"outputs": ["{projectRoot}/node_modules/.tmp"]` to `typecheck` in `nx.json`, or move the `.tsbuildinfo` files somewhere already declared | ⚙️ Tooling & DX | 3 → 5, +0.30 | — |
| Correct `packages/configs/README.md:7-8` — `packages/configs` is the only project with no `vitest.config.ts` | 📚 Documentation | 2 → 4, +0.20 | — |
| Give `apps/tech-docs/README.md` the trade-off section `infrastructure/scripts/README.md:106-112` has, or spec `tools/collect/metrics.ts` and `deps.ts` | 🧪 Testing | 4 → 5, +0.15 | — |
| Add `deny` entries for git write commands (`git commit`, `git push`, `git branch`, `gh pr create`) to back `CLAUDE.md`'s "never raise git" | 🤖 Agent setup | 3 → 4, +0.10 | — |
| Replace the four hand-rolled tables with `UTable`, already a dependency | 🧩 Implementation | 4 → 5, +0.20 | — |
| Correct `docs/guides/change-lifecycle.md:57-60` to name all four CI-only gates | ⚙️ Tooling & DX | secondary, non-capping | — |

Ordered by points per unit of work. This table is a finding, not a tracker: anything worth doing
gets filed (`pnpm issue:add`) or becomes a [spike report](../decisions/README.md), and the row
records which.
