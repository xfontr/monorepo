# 📊 Review — 2026-09-10

Commit `bfd6da2` on `feature/monorepo/65-nuxt-types` · Rubric v1 · Previous:
[2026-09-05](./2026-09-05-abcb17d.md)

A feature branch, so this scores work in progress: 62 commits (172 files, +14301/−2307) landed since
the last review, most of it the `tech-docs` wiki dashboard and its `nx audit`/`nuxt-prepare` wiring.
**4.4/5**, up from 4.2, carried by Architecture and Implementation both holding a clean 5 and
Tooling & DX joining them once this branch's own `nuxt-prepare` fix landed. The one real drop is
📚 Documentation, 4 → 2: `apps/tech-docs/README.md` tells a reader to run `pnpm tech-docs` and
`pnpm tech-docs:collect`, and neither script exists — the real commands, `pnpm dev tech-docs` and
`pnpm exec nx collect @monorepo/tech-docs`, are only in the root README. A one-line fix closes it.

## 🧮 Scores

| Card | Score | Δ | Verdict |
| --- | --- | --- | --- |
| 🧱 Architecture | 5/5 | = | Tag tables agree, the rule is `error`, and a spec now pins the drift the hook also catches |
| 🧩 Implementation | 5/5 | ↑1 | `no-explicit-any` is `error` now, not `warn` — the last review's exact gap is closed |
| 🧪 Testing | 4/5 | = | Error-path coverage is strong; `tech-docs/tools/collect/metrics.ts` and `deps.ts` parsing logic still isn't |
| ⚙️ Tooling & DX | 5/5 | ↑1 | This branch's own `nuxt-prepare` dependsOn wiring is correct and its breakage is nameable |
| 📚 Documentation | 2/5 | ↓2 | `apps/tech-docs/README.md` documents two commands that don't exist |
| 🤖 Agent setup | 3/5 | ↓1 | "Never raise git" is CLAUDE.md's loudest ban and the only one with no deny entry or hook |
| 📋 Process & delivery | 5/5 | ↑1 | Version, commit-type, branch-name and TODO gates are all live and each has a nameable breakage |
| **Total** | **4.4/5** | ↑0.2 | |

Δ is against [2026-09-05](./2026-09-05-abcb17d.md). Weights and the arithmetic live in
[`SCORECARDS.md`](./SCORECARDS.md).

## 🃏 Cards

### 🧱 Architecture — 5/5

The layering is still a property of the code. [`boundaries.ts`](../../packages/configs/src/eslint/lib/boundaries.ts)
and the [root README's table](../../README.md#-architecture--boundaries) list the same tags row for
row, `@nx/enforce-module-boundaries` runs as `"error"`, and grepping `content`/`i18n`'s `core/` for
`@nuxt/*`/`h3`/`nitropack` returns nothing. What's new since the last review: the tag-table sync is
no longer only a hook's job — `apps/tech-docs/tools/lib/invariants.spec.ts:63-82` exercises
`compareTagTables` against a deliberately-drifted pair and asserts the finding, so the same mistake
now fails a spec in addition to `check-invariants.sh`.

| Costs a point | Evidence |
| --- | --- |
| Nothing — sweep found no remaining gap | `packages/configs/src/eslint/lib/boundaries.ts:11-66` vs `README.md:51-60`; `apps/tech-docs/tools/lib/invariants.spec.ts:63-82`; only 4 `process.env` reads repo-wide, all in `infrastructure/translations` and `infrastructure/scripts` |

### 🧩 Implementation — 5/5 (↑1)

The mover. The last review held this at 4 because `no-explicit-any` was only a `warn` inside
`recommendedTypeChecked`, so a regression wouldn't fail anything. It's now `"error"` in
`packages/configs/src/eslint/node.ts:31` and `vue.ts:38`, matching the zero-`any` count
`collect-facts.sh` reports. `content` and `i18n` still diverge only where the domain does —
`packages/content/src/core/adapters/clients/OfetchHttpClient.ts:8-17` reads pagination headers,
`packages/i18n`'s equivalent doesn't need to, and each divergence is a named, single reason rather
than accidental drift. Error paths stay honest: `packages/content/src/core/domain/errors.ts:8-19`
documents exactly why 400/404 pass through and everything else becomes 502, and
`apps/tech-docs/server/utils/store.ts:5-16` explains why a missing snapshot resolves to `null`
instead of throwing.

| Costs a point | Evidence |
| --- | --- |
| Nothing — sweep found no `any`, ts-escape, `eslint-disable`, single-caller abstraction or swallowed error path | `packages/configs/src/eslint/node.ts:31`, `vue.ts:38`; `packages/configs/src/eslint/lib/coreIsolation.ts:11-23` |

### 🧪 Testing — 4/5

367 cases across 51 specs, and the suite still pins failure modes, not method names:
`packages/i18n/src/nuxt/runtime/server/translations.get.spec.ts:58-124` separates 404/500/502 and a
misconfigured-vendor case, and `WordpressProvider.spec.ts:88-92` pins URL-construction edge cases a
one-character regression would break. Providers are exercised through injected `HttpClient` fakes,
never a mock asserted against itself. The gap is new territory this branch added:
`apps/tech-docs/tools/collect/metrics.ts:50-57,93-143` (the weighted-coverage average, the
conventional-commit regex, ESLint-output parsing) and `deps.ts:37-94` (audit/outdated JSON reshaping)
hold real logic with no spec, and unlike `infrastructure/scripts` — which names this exact trade-off
in its README — nothing in `apps/tech-docs/README.md` says so. Swapping `covered`/`total` in
`metrics.ts`'s `weighted()` would pass every existing test.

| Costs a point | Evidence |
| --- | --- |
| Real, non-trivial parsing logic added this branch has no spec and no named reason | `apps/tech-docs/tools/collect/metrics.ts:50-57,93-143`, `deps.ts:37-94`; contrast `infrastructure/scripts/README.md:104-110`, which names the same trade-off for its own untested adapters |

### ⚙️ Tooling & DX — 5/5 (↑1)

This branch's own change is the evidence. `nx.json:20,25,41` now makes `lint`, `typecheck` and `test`
all `dependsOn: nuxt-prepare`, and `apps/huella-legal/tsconfig.json:5-8` only references
`.nuxt/tsconfig.*.json` — a generated path nothing commits — so a fresh clone had no working
typecheck without this wiring. Both Nuxt apps declare the `nuxt-prepare` script
(`apps/huella-legal/package.json:11`, `apps/tech-docs/package.json:9`), so the "two places that must
agree" rule CLAUDE.md names actually agrees. Cache outputs are still declared honestly
(`nx.json:32,47,52` against real `.output`/`.nuxt`/`coverage` dirs), and the pre-push/CI gaps
(`build`, `docs:map --check`) are each named in a doc a reader would find. The breakage is nameable:
drop `nuxt-prepare` from `typecheck`'s `dependsOn` and `vue-tsc` fails on the missing
`.nuxt/tsconfig.*.json` reference, caught by both `.husky/pre-push:159-165` and `ci.yml:32`.

| Costs a point | Evidence |
| --- | --- |
| Nothing — sweep found no undeclared output, undocumented CI gap, or unfixed clean-tree failure | `nx.json:20,25,32,41,47,52`; `.claude/settings.json:37-44`'s `eslint-fix.sh` `PostToolUse` hook |

### 📚 Documentation — 2/5 (↓2)

Capped by the card's own 2-rule: a README contradicting the code. `apps/tech-docs/README.md:8`
("`pnpm tech-docs`") and `:105-106` (`pnpm tech-docs` / `pnpm tech-docs:collect` in the command
table) name scripts that don't exist in `apps/tech-docs/package.json:6-14` — the package only
defines `dev`, `lint`, `nuxt-prepare`, `typecheck`, `test`, `test:coverage`, `collect`. The real
invocations are one file over: root `README.md:112,119-120` gives `pnpm dev tech-docs` and
`pnpm exec nx collect @monorepo/tech-docs`. Everything else checked out — the root architecture
table, `infrastructure/scripts/README.md`'s command table against its `package.json`, and
`tech-docs/README.md`'s "no `build` script" claim are all still accurate. Secondary, non-capping:
`infrastructure/scripts/CLAUDE.md` re-derives layout and naming rules its own
[README.md:9-102](../../infrastructure/scripts/README.md) already states, one step short of the
link-first convention six other `CLAUDE.md`s follow.

| Costs a point | Evidence |
| --- | --- |
| The project's own README documents two dropped/renamed commands | `apps/tech-docs/README.md:8,105-106` vs `apps/tech-docs/package.json:6-14`; correct form only in root `README.md:112,119-120` |
| `infrastructure/scripts/CLAUDE.md` restates its README instead of linking it (non-capping) | `infrastructure/scripts/CLAUDE.md` vs `infrastructure/scripts/README.md:9-102` |

### 🤖 Agent setup — 3/5 (↓1)

Enforcement is unusually strong everywhere it exists — `.claude/hooks/check-invariants.sh:18-114`
blocks exactly the bans CLAUDE.md states in prose, each with a comment naming which rule it maps to,
and every one of the 13 skills' `description` frontmatter matches the trigger prompt it's meant to
catch, linking its package README rather than restating it. What caps the card: CLAUDE.md's own
loudest ban — "never raise git" (branching, committing, pushing, PRs, releases) — has no deny entry
and no hook, unlike every other prose ban in the same file. `.claude/settings.json:4-19`'s `allow`
list covers only read-only git (`status`, `diff`, `log`, `show`, `ls-files`, `grep`, `rev-list`);
`Bash(git commit:*)`, `Bash(git push:*)`, `Bash(git branch:*)` and `Bash(gh pr create:*)` have no
`deny` entry, leaving the ban to prose and the default ask-permission path.

| Costs a point | Evidence |
| --- | --- |
| "Never raise git" has no deny entry and no hook, where one is possible | `CLAUDE.md`'s git-banning bullet; `.claude/settings.json:4-32` has explicit `deny` entries for `CHANGELOG.md`, `docs/FEATURES.md` and `nx release`, but none for a git write command |

### 📋 Process & delivery — 5/5 (↑1)

Every gate-able thing is gated, and each has a nameable breakage. `CHANGELOG.md` files
(`packages/ui/CHANGELOG.md:1-39`) are in the exact `nx release` shape, not hand-authored, and
`.claude/hooks/check-invariants.sh:81-88` diffs a project's `package.json` version against `HEAD` and
exits 2 if it moved by hand. `.husky/commit-msg:12-19` auto-injects the issue number from the branch
name so a subject is never hand-typed, `commitlint --edit` gates the format, and
`.github/workflows/pr-metadata.yml:28-89` pulls the PR's title and board straight from the linked
issue. `docs/spikes/` holds 9 dated reports covering the architectural forks visible in this repo's
history, and grep found zero `TODO`/`FIXME` markers in source — only in docs/hooks describing the
mechanism itself. Four separate breakages are each nameable and each caught: a hand-edited version
fails `check-invariants.sh`, an added `TODO` fails `.husky/pre-push:81-91`, a non-conventional
subject fails `commitlint`, and a mistyped branch fails `pre-push`'s regex.

| Costs a point | Evidence |
| --- | --- |
| Nothing — sweep found no hand-edited version/CHANGELOG, unreported fork, type/diff mismatch, or branch-only work | `.claude/hooks/check-invariants.sh:81-88`; `.husky/commit-msg:12-19`; `docs/spikes/` (9 reports) |

## 🔍 Evidence

| Fact | Value |
| --- | --- |
| Targets, whole workspace | `lint` pass · `typecheck` pass · `test` pass · `build` pass, 9 projects |
| Source files (ts/vue, excl. specs) | 178 |
| Specs / test cases | 51 / 367 |
| `any`, ts escapes, `eslint-disable` in non-spec source | 0 / 0 / 0 |
| Flagged comments (`TODO`/`FIXME`) | 0 |
| Skipped or focused specs · `--passWithNoTests` over real logic | 0 · 0 (2 hits, both over already-specced adapter/domain code) |
| `process.env` reads | 4, all inside `infrastructure/translations` or `infrastructure/scripts` |
| Source files over 200 lines | 6 — largest is `apps/huella-legal/app/pages/articles/index.vue` (288) |
| Files with no sibling spec (filtered) | 67 |
| Commits / diff since the last review | 62 commits, 172 files changed (+14301/−2307) |
| `no-explicit-any` severity (new) | `"error"` in `packages/configs/src/eslint/node.ts:31`, `vue.ts:38` — was `"warn"` at the last review |

## 🎯 What would move the total

| Action | Card | Points | Filed |
| --- | --- | --- | --- |
| Fix `apps/tech-docs/README.md`'s command table (`pnpm tech-docs` → `pnpm dev tech-docs`, `pnpm tech-docs:collect` → `pnpm exec nx collect @monorepo/tech-docs`) | 📚 Documentation | 2 → 4, +0.20 | — |
| Extract the parsing logic in `tech-docs/tools/collect/metrics.ts` and `deps.ts` into tested units, the way `scorecards.ts` already was | 🧪 Testing | 4 → 5, +0.15 | — |
| Add `deny` entries (or a hook) for git write commands (`git commit`, `git push`, `git branch`, `gh pr create`) to back CLAUDE.md's "never raise git" | 🤖 Agent setup | 3 → 4, +0.10 | — |
| Rewrite `infrastructure/scripts/CLAUDE.md` to link-first instead of restating its README | 📚 Documentation | secondary, non-capping | — |

Ordered by points per unit of work. This table is a finding, not a tracker: anything worth doing gets
filed (`pnpm issue:add`) or becomes a [spike report](../spikes/README.md), and the row records which.
