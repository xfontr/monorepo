# 📊 Review — 2026-09-23

Commit `61b6f7f` on `master` · Method v6 · Previous: [2026-09-15](./2026-09-15-aea557e.md)

The tree is dirty in one file that matters: `.husky/pre-push` is emptied to 0 bytes and uncommitted,
so the committed hook is what was scored. Two method deviations to know about. First, all seven card
agents hit a rate limit on the model pinned in `.claude/agents/repo-review-card.md` and were re-run
on a model override. Second, `collect-facts.sh` named the wrong previous review (see 🤖 below).
Because of both, this is not a clean v6 run.

**2.9/5** against 3.9, and the 1.0 is not like-for-like. The previous review was scored under v2,
the method has moved to v6 since, and none of the four bumps re-scored the prior commit. Two caps
carry most of the drop:
- 🧩 Implementation falls to 2 on a failed directory read that turns into a clean result.
- 📋 Process falls to 3 on three accepted decisions that the code reversed with no report saying so.

The biggest single mover is a one-line fix at
[`invariants.ts:104`](../../apps/developer-portal/tools/lib/invariants.ts), worth +0.40. That a
dev-dashboard catch can move the total this much is itself a finding about the rubric: read by
blast radius the total is 3.3 (see [⚖️](#️-the-rubric-overstates-this-drop)).

## 🧮 Scores

| Card | Score | Δ | Verdict |
| --- | --- | --- | --- |
| 🧱 Architecture | 4/5 | ↓1 | Boundaries hold at `error`; observability hands vendor objects to its only caller |
| 🧩 Implementation | 2/5 | ↓2 | A failed `readdir` becomes `[]`, and two invariant checks pass with nothing to check |
| 🧪 Testing | 3/5 | ↓1 | The custom lint rules the architecture leans on have no spec and no stated reason |
| ⚙️ Tooling & DX | 3/5 | = | `typecheck` is still cached without declaring the `.tsbuildinfo` it writes |
| 📚 Documentation | 2/5 | = | Last review's one-line README fix never landed, and `AGENTS.md` now contradicts `nx.json` |
| 🤖 Agent setup | 3/5 | = | "Never raise git" still has no deny entry and no hook; the review's own collector misread history |
| 📋 Process & delivery | 3/5 | ↓2 | Three accepted decisions reversed in code with no supersession; one `feat` bumped five packages for README edits |
| **Total** | **2.9/5** | ↓1.0 | |

Δ is against [2026-09-15](./2026-09-15-aea557e.md), scored under **v2**, so every Δ crosses four
method bumps. The arithmetic is
`(4×20 + 2×20 + 3×15 + 3×15 + 2×10 + 3×10 + 3×10) / 100 = 290/100 = 2.9`. Weights and the
arithmetic live in [`SCORECARDS.md`](./SCORECARDS.md).

## 🃏 Cards

### 🧱 Architecture — 4/5 (↓1)

The layering is still a property of the code:
- `@nx/enforce-module-boundaries` is at `"error"` (`packages/configs/src/eslint/lib/boundaries.ts:8`).
- The ten `depConstraints` rows match `README.md:56-65` tag for tag.
- Two attempted breakages are caught under `ci.yml:32`. Importing `h3` into
  `packages/content/src/core/` fails `coreIsolation.ts:14`, and making `i18n` depend on `content`
  fails `boundaries.ts:43-44`.
- The seven `process.env` reads are all in `infrastructure/*`, in the developer portal's
  `nuxt.config.ts`, and in one Storybook config. None is under a package's `src/`.

`content` and `i18n` share a single port, registry and adapter shape, so the "same problem, two
ways" cap doesn't fire. The card drops a point for `packages/observability`, whose public surface
is the vendor itself. A 4 rather than a 3, because
[`packages/observability/AGENTS.md`](../../packages/observability/AGENTS.md) records this as
deliberate ("not a ports-and-adapters package").

| Costs a point | Evidence |
| --- | --- |
| The telemetry adapter can't be swapped without editing its caller: the entry points return vendor objects, and the app builds OTel spans itself | `packages/observability/src/index.ts:14` returns `Faro`; `node.ts:25` returns `NodeTracerProvider`; `apps/huella-legal/app/plugins/observability.client.ts:11` calls `faro.api.pushError`; `apps/huella-legal/server/plugins/observability.ts:25-81` hand-rolls about 60 lines of server-span code |

### 🧩 Implementation — 2/5 (↓2)

The shared packages are good code. The grep counts are clean: 0 `any`, 0 ts escapes and
0 `eslint-disable`. Each vendor-payload risk has an honest error path:
- Missing pagination headers: `WordpressHelpers.ts:37-43`.
- A missing locale: `TolgeeProvider.ts:24-26`.
- Transport failures become typed errors: `OfetchHttpClient.ts:20-22` in both packages.

The two `HttpClient` ports are different contracts (`{ data, headers }` against a bare `T`), not
duplicated logic. All 16 `console.*` calls trace to CLI output, two build-time warnings, a dev
banner, and the documented Vue patch.

The cap comes from `apps/developer-portal`. `listReviewFiles` turns a failed `readdir` into `[]`.
The history README read turns a failure into `""`. `compareReviewHistory` only checks files
against the table, never the table against files. So if `docs/reviews/` moves, both review
invariants report zero findings, and the scorecards page reports zero reviews, both as success.
[`store.ts:17-22`](../../apps/developer-portal/server/utils/store.ts) records that this same kind of
swallow has already shipped an empty dashboard that reported success once. The last review's
duplicated-tables deduction is also still open.

| Costs a point | Evidence |
| --- | --- |
| **Cap 2.** A failed directory read becomes an empty list, and two invariant checks pass over nothing | `apps/developer-portal/tools/lib/invariants.ts:104` `.catch(() => [] as string[])`; `:133` reads the README with `.catch(() => "")`; `compareReviewHistory` at `:88-97` checks one direction only; `tools/collect/scorecards.ts:19` consumes the same list |
| The same table markup is hand-written four times while `@nuxt/ui` is installed and `UTable` is used nowhere | `apps/developer-portal/app/pages/coverage.vue:58`, `deps.vue:80`, `deps.vue:159`, `scorecards.vue:121`; no `UTable` under `apps/` |

### 🧪 Testing — 3/5 (↓1)

None of the six caps fires:
- 0 skipped or focused specs.
- The highest module-per-spec ratio is `huella-legal` at 3.0.
- Both `--passWithNoTests` hits are in `infrastructure/scripts`, and its README names the
  trade-off.
- Sampled specs keep the real error classes and pin failure branches, and none asserts a mock
  against itself. For example, `packages/i18n/src/nuxt/runtime/server/translations.get.spec.ts:58-109`
  pins the 404, the 500 and the 502-with-cause.

Last review's undeclared gap is closed: `tools/collect/metrics.ts` and `deps.ts` now have specs.

The score is 3, not the agent's 4, because the anchor for a 4 is "the gap is deliberate and written
down", and the main gap isn't written down. `packages/configs` holds the rules that 🧱 Architecture's
5-grade breakages depend on (`coreIsolation`, `layerIsolation`, the boundaries wiring). It has no
spec and no `test` target, and neither its README nor the `writing-tests` skill says why. Narrowing
the `coreIsolation` glob fails nothing, and lint just goes quietly permissive. This is a stricter
reading than the last review applied to an undeclared gap, on purpose: 🧱 got its 4 for writing
its gap down, and the same anchor has to apply here.

| Costs a point | Evidence |
| --- | --- |
| The enforcement code has no spec, no test target and no written reason | `packages/configs/src/eslint/lib/coreIsolation.ts:12`, `layerIsolation.ts:2`, `eslint/vue.ts:70-72`; `packages/configs/package.json:15-18` has no `test`; the collector reports 11 modules and 0 specs; the configs README has no testing section |
| Developer-portal page specs name features, not failures, and bundle several behaviours per `it` | 13 `it("renders …")` titles, e.g. `apps/developer-portal/app/pages/projects.spec.ts:69`, `issues.spec.ts:31` |
| An assertion that can't fail | `apps/developer-portal/app/pages/coverage.spec.ts:32` `toContain("3")`, already satisfied by the "83%" asserted at `:30` |

### ⚙️ Tooling & DX — 3/5 (=)

This is the same cap as last time, on byte-identical config. `nx.json:33-43` gives `typecheck`
`"cache": true` and no `outputs`. Meanwhile `packages/ui/package.json:13` runs `vue-tsc --build`,
and `packages/ui/tsconfig.app.json:16` and `tsconfig.node.json:24` write `.tsbuildinfo` under
`./node_modules/.tmp/`.

The rest holds up:
- `build`, `build-storybook`, `nuxt-prepare` and `test:coverage` declare the outputs they write.
- `docs:map --check` and `review:version --check`, which last review found CI-only, now also run in
  `.husky/pre-commit:23,30`.
- Both pass on this tree, and `build` is the one remaining CI-only gate, named in
  `docs/guides/change-lifecycle.md:60-62`.
- All four targets pass on a clean install.

The `lint` failure the collector hit was a stale local shim, not the code:
`packages/configs/node_modules/.bin/nx` still points at the removed `nx@23.2.0`, and re-linting
around it passed all nine projects. It shows how fragile the `nx eslint:lint` script aliases are,
but it doesn't fire "fails on a clean tree".

| Costs a point | Evidence |
| --- | --- |
| **Cap 3.** A cached target writes outputs it doesn't declare | `nx.json:33-43`; `packages/ui/package.json:13`; `packages/ui/tsconfig.app.json:16`, `tsconfig.node.json:24` |
| Every local Nx run prints a Cloud auth failure, for a service a still-accepted decision rejected (non-capping) | collector output: `Nx Cloud encountered some problems … insufficient access … (code: 401)`; `nx.json:171` `nxCloudId`; `docs/decisions/0005-nx-generators-and-ai-agent-setup.md:102` |
| Two different hook-install instructions (non-capping) | `docs/guides/first-hour.md:10` (`core.hooksPath .husky`) vs `package.json:12` `quick-start` running `husky` (which targets `.husky/_`) |

### 📚 Documentation — 2/5 (=)

Capped by the 2-rule twice.
1. The previous review's top Docs finding, a one-sentence correction, survived 100 commits.
   `packages/configs/README.md:6-7` still says `apps/huella-legal` has no `vitest.config.ts`, and
   the file still exists.
2. A new contradiction sits in the root agent guidance, which both agents load every turn.
   `AGENTS.md:148-150` says no test target is wired and that only `lint` and `typecheck` depend on
   `nuxt-prepare`, but `nx.json:67-69` gives `test` that edge too. Two project READMEs agree with
   `nx.json`, so the docs now disagree with each other as well as with the code.

The rest holds:
- The boundaries table and the layout block match the code.
- The export maps, env var names and the root commands are accurate.
- Every project README argues why, not just what.

| Costs a point | Evidence |
| --- | --- |
| **Cap 2, carried over.** A README says a file doesn't exist, and it does | `packages/configs/README.md:6-7` vs `apps/huella-legal/vitest.config.ts:1-3` |
| **Cap 2, new.** Agent guidance contradicts `nx.json` on how `test` is wired | `AGENTS.md:148-150` vs `nx.json:67-69`, `apps/huella-legal/README.md:21`, `apps/developer-portal/README.md:216` |
| Live docs still describe `check-docs` as a CI gate, and it was deleted | `docs/decisions/0011-docs-system-enforcement.md:161`, `0018-developer-portal-duplicated-facts.md:130`, `0020-review-frontmatter.md:133`, all still `accepted`; the target was deleted in `3414e2c` |
| `AGENTS.md` files restate their README point by point (non-capping) | `infrastructure/scripts/AGENTS.md` vs `README.md:30-37`; `apps/huella-legal/AGENTS.md` vs `README.md:60-64,116-120,146-150`; `packages/observability/AGENTS.md` vs `README.md:56-67` |
| Count drift (non-capping) | `README.md:104` says "both CI workflows" install with `--ignore-scripts`, but three do (`ci.yml:26`, `developer-portal-deploy.yml:38`, `release.yml:41`) |

### 🤖 Agent setup — 3/5 (=)

Enforcement is good where it exists:
- `check-invariants.sh` catches lifecycle scripts, a `build` script under `packages/*`, a
  hand-edited `version`, a URL in source, and a review with no history row.
- The deny list covers `CHANGELOG.md`, `docs/FEATURES.md`, release commands and `.env` reads.
- One deduction from last time is gone: `AGENTS.md:5-6` no longer claims the deny list covers every
  ban.

The cap still fires on the loudest ban. "Never raise git" has no deny entry, no hook, and no
written note that the gap is deliberate. On top of that, the review skill's own collector got the
review history wrong this run, and a live skill claims a check that no longer exists.

| Costs a point | Evidence |
| --- | --- |
| **Cap 3.** An `AGENTS.md` ban with no deny entry and no hook, where a partial one is possible | `AGENTS.md:86-90`; `.claude/settings.json:11-17` (only read-only git verbs), `:21-32` (no git entry); both hooks match `Edit\|Write` only |
| The review's fact collector picks the wrong previous review when two share a date. This run it named `bfd6da2` over `aea557e`, which made "commits since" and age wrong, and that figure is the skill's own guard | `.agents/skills/repo-review/collect-facts.sh:44` (`ls … \| sort \| tail -1`) |
| A live skill tells agents a deleted check enforces uniqueness | `.agents/skills/decision-report/SKILL.md:55` ("`check-docs` fails if they do"); the target was deleted in `3414e2c` |
| The pinned card-agent model has no fallback, so all seven agents failed this run until overridden | `.claude/agents/repo-review-card.md:5`; `repo-review/SKILL.md` names no fallback |
| The instance-ID ban's hook only sees URLs in `.ts/.vue/.json`, so an ID in `nx.json` or a badge UUID in `README.md` passes | `.claude/hooks/check-invariants.sh:97-105`; `nx.json:171`; `README.md` Netlify badge |

### 📋 Process & delivery — 3/5 (↓2)

Releases are still derived rather than typed:
- All five `packages/*` versions match the top entry of their changelog, and each entry has the
  shape `nx release` writes.
- The only release path is `release.yml` (`workflow_dispatch`).
- Commitlint enforces Conventional Commits.

The card falls on both of its 3-caps. The first is that three accepted decisions were reversed in
code with no report or supersession, so each still reads as current:
- **0002** says no agent runs in workflows. `docs-review.yml` now runs a Copilot reviewer in CI.
- **0011** says `check-docs` gates CI. `bf29d16` removed it from CI and `3414e2c` deleted it.
- **0005** records Nx Cloud as rejected. `nx.json:171` adopts it.

The second is a commit type that produced a wrong derived version. `ec8e30c`
(`feat: [144] adjust descriptions, cover with tests`) is a portal feature. Its only package-side
change is 4–5 lines of README description in each of the five `packages/*`, and `nx release` duly
cut a minor release of all five for it. On top of both caps, four method bumps (v2 → v6) landed with none of the
re-scores the rubric owes.

| Costs a point | Evidence |
| --- | --- |
| **Cap 3.** Three resolved forks reversed with no decision report | `docs/decisions/0002-docs-drift-detection.md:57-61` (`accepted`) vs `.github/workflows/docs-review.yml:22-50`; `0011-docs-system-enforcement.md:161` vs `bf29d16`, `3414e2c`; `0005-nx-generators-and-ai-agent-setup.md:102` (`implemented`) vs `nx.json:171` |
| **Cap 3.** A `feat` whose package-side diff is README text bumped five packages a minor | `git show --stat ec8e30c -- packages/`: five `README.md` files at 4–5 lines each, no package source; `packages/*/CHANGELOG.md:1-5` |
| Method bumps v3–v6 landed with no re-score of the previous commit | `docs/reviews/README.md` history table: no row between v2 and v6; `SCORECARDS.md` "a bump re-scores the previous review's commit once" |
| Removing a CI gate was typed `feat`, and `ci:` changes were typed `fix` (non-capping, private projects) | `3414e2c feat: [141] remove check docs`; `ec496f5 fix: [111] oversized docs` touches only `.github/workflows/docs-review.yml` |
| Findings from the last review went unfiled and unfixed across 100 commits | `docs/reviews/2026-09-15-aea557e.md:215-222`: every `Filed` cell is `—`; its three top rows are unchanged at this commit |

## 🔗 Across the cards

One deletion shows up on five cards. `3414e2c` deleted `check-docs`, the target that ran the link
check, the mirrored invariants (tag table, layout block, review history) and the decision-frontmatter
rules in CI. The decisions that established it were never superseded. What's left of those checks
now feeds only the portal's dashboard, where the swallowed `readdir` (🧩) can make them read clean
by accident. It fragments as follows:
- 🧱 The tag-table agreement is no longer gated.
- 🧪 Nothing pins the comparison against the real files.
- 📚 Three decisions still describe the gate.
- 🤖 A skill still promises it.
- 📋 The fork was resolved without a report.

It is scored once, on 📋 Process, where "a resolved fork with no report" is the literal predicate.
The other shards are listed as evidence, not deducted a second time. 🧱 Architecture's 4 rests on
observability alone.

The second pattern is the one the last review named: `apps/developer-portal`. It owns the
Implementation cap, the duplicated tables, the weakest specs, and six of the eight files over 200
lines. Its README now says outright that it is "a mostly vibe-coded app, with low human
intervention", which makes the gap deliberate and written down. It still costs where a literal cap
fires, and that is 🧩.

| Finding | Shows up on | Scored on | Evidence |
| --- | --- | --- | --- |
| `check-docs` was deleted; its guarantees and its decisions were left in place | 🧱, 🧪, 📚, 🤖, 📋 | 📋 Process | `3414e2c`, `bf29d16`; `invariants.ts:19-44` compares only tag names and gates nothing; `0011:161`, `0018:130`, `0020:133`; `decision-report/SKILL.md:55` |
| The developer portal is the unsupervised surface, and it now says so | 🧩, 🧪 | 🧩 Implementation | `invariants.ts:104`; `coverage.vue:58`, `deps.vue:80,159`, `scorecards.vue:121`; `coverage.spec.ts:32`; 6 of 8 files over 200 lines; `apps/developer-portal/README.md` "mostly vibe-coded" note |

## 🩺 Beyond the rubric

None of this moves a score. Caps only see what their predicates name, so here are the rest: the
seven card agents' notes and the reconcile pass, each cited.

### 💥 Broken, and no cap sees it

| Finding | Why it matters | Evidence |
| --- | --- | --- |
| The local push gate is off | This clone has pushed with no branch check, no TODO block and no lint/test/typecheck for three days | `.husky/pre-push` is 0 bytes, mtime 2026-09-20 12:01 |
| Review findings don't turn into work | Every `Filed` cell in the last review is `—`, and its top three fixes survived 100 commits. This series is producing data, not changes | `2026-09-15-aea557e.md:215-222` |
| The instance-ID ban is broken by the author, in files the hook can't see | The rule is either wrong or not followed; either way an agent reads it as absolute and watches it not hold | `nx.json:171` `nxCloudId`; the Netlify badge UUID in `README.md`; `check-invariants.sh:97-105` matches URLs in `.ts/.vue/.json` only |
| The issues page silently caps at 100 | The 101st open issue disappears from the count and from "next" | `apps/developer-portal/app/composables/useIssues.ts:7,24` (`PER_PAGE = 100`, no paging) |
| A corrupt artifact reads as "never collected" | A parse error and a fresh clone render identically, the same shape as the incident `store.ts` itself records | `apps/developer-portal/server/utils/store.ts:8-15` |
| A new top-level directory drops out of the wiki nav with no error | Unchecked cast, then a filter against a fixed list | `apps/developer-portal/shared/wiki.ts:185` `segments[0] as SectionId` |

### 🍒 Low-hanging fruit

| Fix | Cost | Evidence |
| --- | --- | --- |
| Let `readdir` throw at `invariants.ts:104` and drop the `.catch(() => "")` on the history read | Two lines | `apps/developer-portal/tools/lib/invariants.ts:104,133` |
| Correct the two contradicting doc lines, then `pnpm agents:sync` | Two sentences | `packages/configs/README.md:6-7`; `AGENTS.md:148-150` |
| Declare `typecheck`'s outputs | One line | `nx.json:33-43` |
| Pick the previous review by commit time | One line | `.agents/skills/repo-review/collect-facts.sh:44` |
| Drop the `nx eslint:lint` script aliases so the inferred target is `lint`; stale shims stop mattering | Nine `package.json` lines | `packages/configs/package.json:16`; `nx.json:136-139` |
| Drop `--passWithNoTests` where 48 specs exist; today deleting every spec stays green | One flag, twice | `infrastructure/scripts/package.json:22,24` |
| Refresh `writing-tests`: "32 specs" is now 131, and the `Button.spec.ts` scaffolding it describes was fixed | Two sentences | `.agents/skills/writing-tests/SKILL.md:8,79-82` |
| Give `@monorepo/configs` a description; every project consumes it | One string | `packages/configs/package.json:4` (`""`) |
| Add `.github/ISSUE_TEMPLATE/`; the three templates live only in a skill, so web-filed issues get none | Three files | no `.github/ISSUE_TEMPLATE/`; `.agents/skills/github-issue/SKILL.md` |
| Triage the eight `to-implement` decisions into issues or `wont-implement`; 0002's follow-ups say "none filed yet" | An hour | `docs/decisions/*.md` frontmatter; `0002-docs-drift-detection.md:63-64` |
| Split `quick-start`: installing hooks shouldn't cost a full coverage run | One script | `package.json:12` |

### 🏗 Over-engineered

| Finding | Evidence |
| --- | --- |
| The review machinery outweighs the product it reviews by almost 3×. About 1,180 lines of prose (skill, collector, card agent, `SCORECARDS`/`METHOD`/`TEMPLATE`, decisions 0010/0012/0020) plus about 720 lines of code (`review-version`, the portal's scorecards collector, parser and page), against about 685 non-spec lines in `packages/ui/lib` and `huella-legal`'s `app/` and `server/`. The method moved v2 → v6 in eight days, faster than the repo it measures | line counts over `git ls-files` at `61b6f7f`; `docs/reviews/README.md` history table |
| Docs roughly equal code. About 10,400 lines of markdown (changelogs and reviews excluded) against about 10,800 lines of non-spec `.ts`/`.vue`. Root `AGENTS.md` (175 lines) loads on every agent turn, including sections it calls reference-only; the portal README is 327 lines for an app it calls vibe-coded, with change history a PR should hold | `AGENTS.md:117-156`; `apps/developer-portal/README.md:66,163-164` |
| Six `AGENTS.md` files restate their README, and the `nx.json` contradiction above is that duplication already drifting | 📚 findings |
| About 180 lines of Node inline in a workflow's YAML, `continue-on-error`, no spec | `.github/workflows/docs-review.yml:55-238` |
| A 264-line validator that nothing runs | `infrastructure/scripts/src/package-contracts/domain/validate.ts`; no reference in `.github/` or `.husky/` |
| Five inferring Nx plugins create renamed duplicate `build`/`typecheck`/`test` targets beside the hand-written scripts; 0015 already records one of them that can't run without Cloud | `nx.json:123-170`; `docs/decisions/0015-nuxt-prepare-wiring.md:107` |

### 🧱 Structural debt

| Finding | Evidence |
| --- | --- |
| `content` and `i18n` each carry their own `HttpClient` port, ofetch client, `UpstreamError` and a near-identical `registry.ts`; the tag graph gives shared transport nowhere to live, so a third vendor package makes three copies | `packages/content/src/core/registry.ts:1-37` vs `packages/i18n/src/core/registry.ts:1-43` |
| "`core/domain` imports nothing" is convention only: `coreIsolation` bans Nuxt/Nitro/h3, not `ofetch` | `packages/configs/src/eslint/lib/coreIsolation.ts:1-9`; `packages/content/AGENTS.md` |
| The two apps follow opposite env rules with no note saying why | `apps/developer-portal/nuxt.config.ts:37` reads `process.env`; `apps/huella-legal/AGENTS.md` bans that |

### ⚖️ The rubric overstates this drop

Caps are predicates with no weight for severity or scope, so one line can decide a 20-weight card.
The `.catch` at `invariants.ts:104` sits in a dashboard collector in a private app that gates
nothing, and it costs 🧩 two points and the total 0.40, the largest single movement in this review.
The last review has the same shape: one missing `outputs` array took ⚙️ from 5 to 3.

Read by blast radius instead, 🧩 is a 4 (the table duplication), and the total is **3.3**, not 2.9.
Most of what's left of the drop is the 📋 caps plus the v2 → v6 method change. Caps scoped to
shipped code and gates (`packages/*`, app runtime, CI), with dev-tooling findings as plain
deductions, would fix it. That is a `SCORECARDS.md` change: `pnpm review:version` bumps the method,
and a re-score of this commit is owed.

## 🔍 Evidence

| Fact | Value |
| --- | --- |
| Targets, whole workspace | `typecheck` pass · `test` pass · `build` pass · `lint` failed on a stale local `nx@23.2.0` shim in `packages/configs`, then passed on all 9 projects when re-run around it |
| Gates run by hand | `review:version --check` pass (v6, four artifacts unchanged) · `docs:map --check` pass · `package:check` pass, and nothing runs it automatically |
| Source files (ts/vue, excl. specs) | 209 (was 189) |
| Specs / test cases | 131 / 702 (was 54 / 407) |
| Modules per spec, highest | `huella-legal` 3.0; `configs` has 11 modules and no specs |
| `any` · ts escapes · `eslint-disable` · flagged comments | 0 · 0 · 0 · 0 |
| `console.*` outside specs | 16, all traced |
| `process.env` reads | 7, none in a package's `src/` |
| Source files over 200 lines | 8, six in `apps/developer-portal` |
| Since the previous review's commit (`aea557e`) | 100 commits, 360 files, +15629/−3910. The collector reported 128 commits against `bfd6da2`, which is the wrong baseline |
| Working tree | `.husky/pre-push` is 0 bytes, uncommitted, mtime 2026-09-20 12:01, one minute after `3414e2c` |

## 🎯 What would move the total

| Action | Card | Points | Filed |
| --- | --- | --- | --- |
| Let `readdir` at `invariants.ts:104` throw, or turn ENOENT into a finding; drop the `.catch(() => "")` on the history read | 🧩 Implementation | 2 → 4, +0.40 | — |
| Fix `packages/configs/README.md:6-7` (only `packages/configs` lacks a `vitest.config.ts`) and `AGENTS.md:148-150` (`test` depends on `nuxt-prepare` too), then `pnpm agents:sync` | 📚 Documentation | 2 → 4, +0.20 | — |
| Write one report that supersedes 0002, 0005 and 0011 for the Copilot reviewer, Nx Cloud and the `check-docs` removal, and set `decision: superseded` on each | 📋 Process | 3 → 4, +0.10, once the `ec8e30c` cap also clears (it leaves the window at the next review) | — |
| Add `outputs: ["{projectRoot}/node_modules/.tmp"]` to `typecheck` in `nx.json` | ⚙️ Tooling & DX | 3 → 4, +0.15 | — |
| Add a "✅ Tests" line to `packages/configs/README.md` naming the unpinned rules, or give it one `ESLint#lintText` spec per custom rule | 🧪 Testing | 3 → 4, +0.15 | — |
| Add `deny` entries for `git commit`, `git push`, `git checkout -b` and `gh pr create`, or record the gap as deferred | 🤖 Agent setup | 3 → 4, +0.10 | — |
| Pick the previous review in `collect-facts.sh:44` by commit time, not by `sort` | 🤖 Agent setup | secondary, non-capping | — |
| Replace the four hand-rolled tables with `UTable` | 🧩 Implementation | 4 → 5 once the cap is lifted, +0.20 | — |

Ordered by points per unit of work. This table is a finding, not a tracker: anything worth doing
gets filed (`pnpm issue:add`) or becomes a [decision report](../decisions/README.md), and the row
records which.
